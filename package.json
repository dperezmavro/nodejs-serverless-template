{
  "name": "webhook_test",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "coverage": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha",
    "deploy": "npm test && npm run-script make && cd build && sls deploy",
    "make": "rm -rf ./build && mkdir build && cp serverless.yml package.json build/ && ./node_modules/.bin/webpack -p && cd build && npm install --production",
    "test": "./node_modules/mocha/bin/mocha"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-loader": "^6.4.0",
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-stage-0": "^6.22.0",
    "commander": "^2.9.0",
    "expect": "^1.20.2",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "mocha-istanbul": "^0.3.0",
    "sinon": "^1.17.7",
    "uuid": "^3.0.1",
    "webpack": "^2.2.1",
    "webpack-node-externals": "^1.5.4"
  }
}
